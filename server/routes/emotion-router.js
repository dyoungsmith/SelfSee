var express = require('express');
var router = express.Router();
var Selfie = require('../models/selfie-model.js');

// GET emotion for particular selfie
router.get('/', function(req, res, next) {

});


module.exports = router;